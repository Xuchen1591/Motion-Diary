<Page
    x:Name="pageRoot"
    x:Class="motion_diary_v2.LogoPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:motion_diary_v2"
    xmlns:common="using:motion_diary_v2.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40" Text="Motion Diary"/>
        </Grid>
        <TextBlock HorizontalAlignment="Center" Margin="380,60,379,0" TextWrapping="Wrap" Text="would u like choose a logo for U !" VerticalAlignment="Top" Height="47" Width="607" FontFamily="Arial Unicode MS" FontSize="36" TextAlignment="Center" Grid.Row="1"/>
        <Image x:Name="Logo" HorizontalAlignment="Left" Height="330" Margin="516,128,0,0" Grid.Row="1" VerticalAlignment="Top" Width="330" Source="Assets/Logo.png"/>
        <Button x:ConnectionId='1' Content="pick" HorizontalAlignment="Left" Margin="513,486,0,0" Grid.Row="1" VerticalAlignment="Top" Height="50" Width="140" FontSize="24"                       />
        <Button Content="save" HorizontalAlignment="Left" Margin="709,486,0,0" Grid.Row="1" VerticalAlignment="Top" Height="50" Width="140" FontSize="24"/>
        <TextBlock x:Name="hiUserName" HorizontalAlignment="Center" Margin="380,8,379,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="47" Width="607" FontFamily="Arial Unicode MS" FontSize="36" TextAlignment="Center" Grid.Row="1"/>
        <Button x:ConnectionId='2' Content="Button" HorizontalAlignment="Left" Margin="1198,533,0,0" Grid.Row="1" VerticalAlignment="Top"                       />
    </Grid>
</Page>

